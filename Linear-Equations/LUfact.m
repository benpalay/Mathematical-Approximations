function [P,L,U] = LUfact(A)
[n m] = size(A); %n is number of rows and m is number of columns of an n x m matrix;
[s f] = size (B);

X= zeros([s f]); %returns a sxf 0 matrix
   V=zeros[n,1];
   for l=1:n
    V(l)=1;
   end
   
   Q = diag(V)
   disp(Q)
   
  
end